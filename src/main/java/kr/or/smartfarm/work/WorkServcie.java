package kr.or.smartfarm.work;

/**
 * WorkServcie - 오타로 생성된 빈 클래스 (사용되지 않음).
 *
 * 주의: 실제 서비스 인터페이스는 WorkService.java 이며,
 *       구현 클래스는 WorkServiceImpl.java 이다.
 *       이 파일은 "WorkService" 를 잘못 입력하여 생성된 아티팩트(artifact)로,
 *       아무런 로직도 포함하지 않는다. 삭제해도 무방하다.
 */
public class WorkServcie {

}
