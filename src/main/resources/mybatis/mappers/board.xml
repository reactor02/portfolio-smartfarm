<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.board">

<select id="selectBoard" resultType="kr.or.smartfarm.board.BoardDTO">
      select * from board
    </select>

<select id="selectOneBoard" resultType="kr.or.smartfarm.board.BoardDTO">
	select * from board where board_num = #{board_num}
</select>

<insert id="insertBoard">
	insert into board (board_num, category, title, content, created_at)
	values (board_seq.nextval,#{category},#{title}, #{content}, sysdate)
</insert>

<select id="findById" resultType="kr.or.smartfarm.board.BoardDTO"> 
	select board_num, title, content from board where board_num = #{board_num}
</select>

</mapper>


