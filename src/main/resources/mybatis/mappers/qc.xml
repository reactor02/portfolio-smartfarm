<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="mapper.qc">

<!-- modelConfig.xml 에서 세팅해서 별칭 만들었음 -->

	<select id="selectAllQc" resultType="QcDTO">
		SELECT
		    io.io_num,
		    io.io_qty,
		    io.io_date,
		    lot.lot_code,
		    emp.ename,
		    qc.qc_type,
		    qc.qc_pass,
		    item.code,
		    item.name,
		    item.unit
		FROM io
		JOIN qc
		    ON qc.qc_num = io.qc_num
		JOIN emp
		    ON io.emp_num = emp.emp_num
		JOIN lot
		    ON io.lot_num = lot.lot_num
		JOIN item
		    ON lot.item_num = item.item_num
		ORDER BY io.io_date DESC
	</select>
	
<!-- Select Item -->
	<select id="selectItem" resultType="QcDTO">
		SELECT 
			item_num,
			code,
			name
		FROM item
	</select>
	
	<!-- 검색 조건에 맞게 검색 -->
	<select id="searchQc" resultType="hashMap" parameterType="map">
		SELECT
		    io.io_num,
		    io.io_qty,
		    io.io_date,
		    emp.ename,
		    lot.lot_code,
		    qc.qc_type,
		    qc.qc_pass,
		    item.code,
		    item.name,
		    item.unit
		FROM io
		JOIN qc
		    ON qc.qc_num = io.qc_num
		JOIN emp
		    ON io.emp_num = emp.emp_num
		JOIN lot
		    ON io.lot_num = lot.lot_num
		JOIN item
		    ON lot.item_num = item.item_num
		
	    <where>
	    
		<!-- 날짜 조회 -->
	     <if test="sDate != null and sDate != '' and eDate != null and eDate != ''">
		    AND io.io_date >= TO_DATE(#{sDate}, 'YYYY-MM-DD')
		    AND io.io_date &lt; TO_DATE(#{eDate}, 'YYYY-MM-DD') + 1
		</if>
		<!-- item_num 검색 -->
	        <if test="type != null and type != '' and type != 'all'">
				AND qc.qc_pass = #{type}
			</if>
	    <!-- 품목명 검색 --> 
	        <if test="keyword != null and keyword != ''">
	        	AND (
		        	upper(item.name) LIKE ('%' || upper(#{keyword}) || '%')
		         OR upper(item.code) LIKE ('%' || upper(#{keyword}) || '%')
		         )
	   	 	</if>
	    </where>
	    
	  	ORDER BY io.io_date DESC
    </select>
    
    <select id="selectWaiting" resultType="QcDTO">
		SELECT
		    io.io_num,
		    io.io_qty,
		    io.io_date,
		    emp.ename,
		    lot.lot_num,
		    lot.lot_code,
		    qc.qc_type,
		    qc.qc_pass,
		    item.code,
		    item.name,
		    item.unit
		FROM io
		JOIN qc
		    ON qc.qc_num = io.qc_num
		JOIN emp
		    ON io.emp_num = emp.emp_num
		JOIN lot
		    ON io.lot_num = lot.lot_num
		JOIN item
		    ON lot.item_num = item.item_num
		WHERE qc.qc_pass = 'WAITING'
		ORDER BY io.io_date DESC
	</select>
	
	 <select id="selectDetail" resultType="QcDTO" parameterType="int">
		SELECT
		    io.io_num,
		    io.io_qty,
		    io.io_date,
		    emp.ename,
		    lot.lot_num,
		    lot.lot_code,
		    qc.qc_type,
		    qc.qc_pass,
		    item.code,
		    item.name,
		    item.unit
		FROM io
		JOIN qc
		    ON qc.qc_num = io.qc_num
		JOIN emp
		    ON io.emp_num = emp.emp_num
		JOIN lot
		    ON io.lot_num = lot.lot_num
		JOIN item
		    ON lot.item_num = item.item_num
		WHERE io.io_num = #{io_num}
		ORDER BY io.io_date DESC
	</select>
		
	<select id="selectLog" resultType="QcDTO" parameterType="int">
		SELECT
		    io.io_num,
		    lot.lot_code,
		    item.name,
		    io.io_qty,
		    item.unit,
		    io.io_date,
		    qc.qc_pass,
		    emp.ename
		FROM io
		JOIN qc
		    ON qc.qc_num = io.qc_num
		JOIN emp
		    ON io.emp_num = emp.emp_num
		JOIN lot
		    ON io.lot_num = lot.lot_num
		JOIN item
		    ON lot.item_num = item.item_num
		WHERE lot.lot_num = 
		(
			SELECT lot_num FROM io
			WHERE io_num = #{io_num}
		)
		ORDER BY io.io_date DESC
	</select>
	
	<select id="qcList" resultType="QcDTO">
		SELECT * from qc
	</select>
	
	<select id="qcChk" resultType="QcDTO" parameterType="int">
		SELECT qc_pass from qc
		WHERE qc_num = #{qc_num}
	</select>
	
		
	
</mapper>




